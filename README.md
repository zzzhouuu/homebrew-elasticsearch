# Zzzhouuu Elasticsearch

## How do I install these formulae?

`brew install zzzhouuu/elasticsearch/<formula>`

Or `brew tap zzzhouuu/elasticsearch` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
